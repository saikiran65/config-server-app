git branch >> to see the hooked branch and list of branches
git switch >> to change the branch

git push --set-upstream origin brn1 (here origin is main) >> we are setting up the upstream branch for brn1
